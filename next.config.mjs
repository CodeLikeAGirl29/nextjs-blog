/** @type {import('next').NextConfig} */
const nextConfig = {
  // No `output: "export"` here — Vercel runs Next.js natively, so you get
  // image optimization, ISR, etc. for free. If you ever need a fully
  // static export instead (e.g. GitHub Pages), add `output: "export"` back.
};

export default nextConfig;
