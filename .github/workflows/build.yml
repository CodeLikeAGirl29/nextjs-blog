- name: Build static site
  run: npm run build

- name: Verify build output
  run: |
    if [ ! -d "./out" ]; then
      echo "Error: ./out directory not found after build"
      ls -la
      exit 1
    fi